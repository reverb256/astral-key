# DEPRECATED — superseded by the Helm chart (2026-09-22).
#
# This raw manifest was applied imperatively via `kubectl apply -f`. The
# service is now managed declaratively by ArgoCD:
#   - chart:   charts/astral-key/  (this repo)
#   - app:     astral-key (argocd ns, via media-helm app-of-apps,
#              helm/apps/astral-key.yaml in reverb256/media-k8s)
#   - deploy key: astral-key-repo (read-only)
#
# The rationale formerly documented here (env names matching src/config.rs,
# hostPath /var/lib/astral-key instead of a PVC, JWT_SECRET requirement)
# now lives in charts/astral-key/values.yaml and the chart templates.
#
# Do NOT kubectl apply this file. To change the service, edit the chart
# values and commit — ArgoCD syncs automatically.
apiVersion: v1
kind: ConfigMap
metadata:
  name: astral-key-deployment-deprecated
  namespace: astral-key
  labels:
    deprecated: "true"
data:
  note: "Superseded by charts/astral-key (ArgoCD app: astral-key). See file header."
